useState is the function known as hook of react that renders the js variable upon update

use state returns an array of two variables-->one is your set variable and other is the function..that function can render the dynamics of that variable

just call the function provided by useState (give it your own name) whenever you update that variable...rest is upto react.. 